нужно перебиндживать сигнаоы
	Приглашение ввода дважды выводится после прерывания процесса через Ctrl-C
	Это вроде из-за того что нужно как то по особому обрабатывать сигналы в уже запущенных прогах

	Ctrl-\ не убивает
+exit_minishell()

fork_cmd() -= access()
#include <libft.h> -> "libft.h"
make re = make clean && make
    ("make re" errors on commit "- #include <bits/sigaction.h>" after next)
[norm] libft/error.h: e_ft_err -= uppercase += explicit
Makefile -= *
Makefile -= printf
Makefile += color on Ubuntu
fork_cmd.c -= #include <sys/types.h>
[rename] ft_true -> true

-.vscode
git -= submodules

?Ctrl-R Ctrl-C
